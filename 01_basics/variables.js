const accountID = 1234465;
//  const variable cannot be change once it's have been declared

let accountEmail = 'ajha@google.com';
// if we declare the variable let then it can only be accessilbe with their scope

var accountPassword = '12455';
// if we declare the varialbe with var then it can we changed & it will create problems in code. 

/* Const & let - ES6(introduce in)
var - ES5 */

// console.log(accountEmail)
// console.table([accountID,accountEmail,accountPassword])